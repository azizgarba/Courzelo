pipeline {
   agent any 
   stages {
      stage('Build') {
        steps {
            sh "mvn -version"
        }
      }
         stage('Build') {
        steps {
            sh "java -version"
        }
      }
   }
}
